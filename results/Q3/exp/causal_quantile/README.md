Causal full-year ablations for 48 h look-ahead and three-segment net-load quantiles.

`full_year_comparison.csv` is the summary. Each `causal_h*_q*` / `causal_h48_beta` directory is one `run_q3.py --phase official --no-export` run after `74ba2ba` (week-similar PV tail). Do not mix with `quantile_sweep/` or `quantile_edges/`, which used tomorrow's unpublished 0:00 PV issue.
